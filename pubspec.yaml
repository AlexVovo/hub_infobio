name: hub_infobio
description: "Hub de projetos de Bioinformática"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # Ícones adicionais (DNA, etc.)
  font_awesome_flutter: ^10.7.0

  # Ícones padrão iOS
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  

flutter:
  uses-material-design: true
